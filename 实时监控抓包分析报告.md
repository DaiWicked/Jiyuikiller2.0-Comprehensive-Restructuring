# 极域V6.0 实时监控抓包分析报告

## 一、抓包概述

### 三次抓包对比

| 抓包 | 文件 | 包数 | 时长 | 结果 |
|------|------|------|------|------|
| 第一次 | realtime_view.pcapng | 335 | 50秒 | 只有80x60缩略图 |
| 第二次 | realtime_fullscreen.pcapng | 762 | 2分钟 | 只有80x60缩略图 |
| **第三次** | **realtime_fullscreen2.pcapng** | **22792** | **约70秒** | **找到实时屏幕协议！** |

### 第三次抓包基本信息

- **抓包文件**: realtime_fullscreen2.pcapng
- **总包数**: 22792个（全部UDP）
- **教师端IP**: 192.168.3.96
- **学生端IP**: 192.168.3.148（Win7 32位）
- **关键端口**: UDP 5575（实时查看）、UDP 5566（屏幕广播）

---

## 二、端口与协议分布

### 所有IP:端口对

| 源:端口 -> 目的:端口 | 包数 | 作用 |
|----------------------|------|------|
| 学生:5563 -> 225.2.2.29:5563 | 12970 | 组播心跳/帧头 |
| 学生:5572 -> 225.2.2.35:5572 | 6302 | 组播心跳/帧头 |
| **学生:55478 -> 教师:5575** | **2518** | **实时屏幕数据** |
| 教师:61282 -> 广播:4705 | 328 | 主通信心跳 |
| 学生:61843 -> 教师:5512 | 164 | ANNO控制 |
| 学生:61844 -> 教师:4809 | 157 | NIPQ心跳 |
| **学生:62570 -> 教师:5566** | **140** | **屏幕广播数据** |
| 教师:61283 -> 225.2.2.1:5512 | 112 | 组播控制 |
| 学生:55478 -> 教师:5576 | 58 | 确认包 |
| 学生:55993 -> 教师:4705 | 30 | 缩略图(DENT/LANT) |

### 包类型统计（第三次抓包）

| 包类型 | 数量 | 最大大小 | 说明 |
|--------|------|----------|------|
| (空magic) | 19272 | 47字节 | 5563/5572组播心跳 |
| >, (0x3e2c) | 2576 | 1453字节 | **5575实时屏幕数据** |
| ANNO | 204 | 72字节 | 5512端口控制 |
| OONC | 158 | 44字节 | 4705心跳 |
| CANC | 158 | 112字节 | 4705心跳 |
| NIPQ | 157 | 43字节 | 4809心跳 |
| #w (0x2377) | 140 | 1453字节 | **5566屏幕广播数据** |
| MESS | 72 | 633字节 | 消息 |
| LANT | 18 | 1073字节 | 4705缩略图 |
| DENT | 8 | 40字节 | 4705缩略图帧头 |

---

## 三、实时屏幕协议（5575端口）

### 3.1 基本参数

| 参数 | 值 |
|------|-----|
| **分辨率** | **1152 x 768** |
| **帧率** | 约26fps（470帧/18秒） |
| **每帧大小** | 约2900字节（压缩后） |
| **传输方式** | UDP，每帧重传2次 |
| **数据格式** | **自定义视频编码**（非JPEG/Zlib） |

### 3.2 包结构（56字节header + payload）

```
偏移  长度  内容                  示例值
0     2     标识                  3e 2c (">,")  [5575] / 23 77 ("#w") [5566]
2     2     保留                  00 00
4     4     帧序号（小端）         02 00 00 00 = 2
8     2     保留                  00 00
10    2     分片/通道ID           a0 02 = 40962
12    4     时间戳/随机数         c5 48 48 52
16    4     未知                  46 ff 87 00
20    4     未知                  86 65 2f 00
24    8     保留                  00...
32    4     宽度（小端）          80 04 00 00 = 1152
36    4     高度（小端）          00 03 00 00 = 768
40    8     保留                  00...
48    4     参数1                 7e 04 00 00 = 1150
52    4     参数2                 f7 02 00 00 = 759
56+   N     压缩数据              ...
```

### 3.3 帧类型

| 帧序号 | 大小 | 类型 | 说明 |
|--------|------|------|------|
| 0 | 278字节 | 初始化帧 | 包含参数，payload全0 |
| 2 | 2906字节 | I帧/关键帧 | 第一个完整帧，payload开头`0001` |
| 3+ | 2906字节 | P帧/差分帧 | 增量帧，直接是压缩数据 |

### 3.4 压缩数据特征

- **不是标准JPEG**（无FF D8开头）
- **不是Zlib**（无78 9C头）
- 帧3+的payload开头：`85 a8 27 f7 f7 66 53 2a...`
- 可能是极域自定义的差分视频编码
- 每帧约2900字节压缩后 = 1152x768x3 = 2.6MB原始数据，压缩比约900:1

---

## 四、缩略图协议（4705端口）

### 4.1 DENT包结构（40字节）

```
偏移  长度  内容              示例值
0     4     Magic "DENT"      44 45 4e 54
4     4     版本/标志         00 00 01 00
8     4     payload_len       0c 00 00 00 (12字节)
12    16    会话ID/哈希       aa 3a 8d be ...
28    4     学生IP            c0 a8 03 94
32    4     学生IP(重复)      c0 a8 03 94
36    4     frame_seq         59 01 00 00 (345)
```

### 4.2 LANT包结构（48字节header + payload）

```
偏移  长度  内容              示例值
0     4     Magic "LANT"      4c 41 4e 54
4     4     版本/标志         00 00 01 00
8     4     payload_len       15 04 00 00
12    16    会话ID/哈希       aa 3a 8d be ...
28    4     学生IP            c0 a8 03 94
32    4     frame_seq         59 01 00 00
36    4     分片序号          81 08 00 00
40    4     分片偏移          00 00 00 00 / 00 04 00 00 / 00 08 00 00
44    4     分片大小          00 04 00 00 (1024)
48+   N     JPEG数据          ff d8 ff db ...
```

### 4.3 缩略图参数

- **分辨率**: 80x60
- **格式**: JPEG（FF D8 FF DB开头，FF D9结尾）
- **分片**: 每片1024字节，一帧分3片
- **重传**: 每包重传2次
- **更新频率**: 约15-18秒/帧（非实时）

---

## 五、其他协议

### 5.1 NIPQ心跳（4809端口，43字节）

```
4e 49 50 51 00 00 01 00 0f 00 00 00 [16字节哈希] c0 a8 03 94 [8字节MAC/额外]
```

- 学生端→教师端，每6秒一次
- 第三次抓包多了8字节：`00 0c 29 3e 0c 30 00 00`（可能是VMware网卡MAC）

### 5.2 ANNO控制（5512端口，72字节）

```
41 4e 4e 4f 01 00 00 00 [标志] [16字节] c0 a8 03 94 ...
```

- 教师端和学生端都有
- 可能是"公告/通知"控制通道

### 5.3 组播心跳（5563/5572端口，47字节）

```
c0 a8 03 94 00 00 00 00 0a 00 00 00 07 00 00 00 01 00 00 00 [28字节0]
```

- 学生端→组播地址(225.2.2.29/225.2.2.35)
- 高频发送（约每秒多次）
- 可能是屏幕广播的帧同步/心跳

---

## 六、截图替换方案评估

### 6.1 已实现：缩略图替换

| 项目 | 状态 |
|------|------|
| 替换位置 | EncodeToJPEGBuffer函数（编码层） |
| 替换目标 | 80x60缩略图JPEG |
| 实现方式 | DLL注入 + hook编码函数 |
| 稳定性 | 高（不触碰GDI/驱动） |
| 效果 | 教师端学生列表显示假截图 |

### 6.2 未实现：实时大屏幕替换

| 方案 | 难度 | 可行性 | 说明 |
|------|------|--------|------|
| **网络层替换** | **极高** | ❌ | 需逆向自定义视频编码，理解I/P帧、差分算法 |
| Hook屏幕捕获 | 中 | ⚠️ | 可能影响稳定性，32位Win10驱动兼容性问题 |
| Hook编码函数 | 中 | ⚠️ | 需找到实时屏幕的编码函数（非EncodeToJPEGBuffer） |
| 假桌面窗口 | 高 | ❌ | 原作者方案，32位Win10易崩溃 |

### 6.3 结论

1. **缩略图替换已完成**，可稳定替换教师端学生列表的80x60截图
2. **实时大屏幕替换难度极高**，因为极域用自定义视频编码（非标准JPEG）
3. 网络层替换需要完全逆向自定义编码格式，工作量巨大
4. 建议：保持缩略图替换，实时大屏幕作为长期预研项

---

## 七、后续预研方向

### 7.1 自定义视频编码逆向

- 找到学生端负责实时屏幕编码的DLL/函数
- 分析编码算法（可能是基于差分的轻量视频编码）
- 尝试构造假帧数据

### 7.2 Hook屏幕捕获

- 定位学生端的屏幕捕获函数（BitBlt/CopyFromScreen等）
- 在捕获层替换屏幕数据
- 风险：可能触发驱动级保护

### 7.3 教师端解码分析

- 分析教师端如何解码5575端口数据
- 找到解码函数，理解编码格式
- 可能比逆向编码更容易

---

## 八、附录

### 8.1 提取的JPEG缩略图

- `realtime_frames/`: 第一次抓包提取的80x60缩略图（3帧）
- `realtime_fullscreen_frames/`: 第二次抓包提取的80x60缩略图（5帧）
- `realtime_fullscreen2_frames/`: 第三次抓包提取的80x60缩略图（3帧）

### 8.2 工具

- Wireshark/tshark: `C:\Program Files\Wireshark\tshark.exe`
- 抓包过滤: `ip.addr == 学生IP`（捕获所有端口）
- 关键发现：实时查看用5575端口，不是4705端口

### 8.3 关键时间线（第三次抓包）

```
17:56:12  教师端/学生端登录，4705端口心跳开始
17:56:14  学生端发送DENT/LANT缩略图（4705端口）
17:56:19  5566端口开始发送屏幕广播数据
17:56:20  5563/5572组播心跳开始
17:57:03  **5575端口开始发送实时屏幕数据**（帧0初始化）
17:57:03  帧2（I帧）、帧3+（P帧）持续发送
17:57:21  抓包结束，共470帧实时屏幕数据
```

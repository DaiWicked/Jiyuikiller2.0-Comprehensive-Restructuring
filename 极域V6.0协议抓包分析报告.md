# 极域V6.0协议抓包分析报告

## 分析环境
- 教师端IP: 192.168.3.96 (真实教师端 V6.0 2016豪华版)
- 学生端1: 192.168.3.148 (Win7 32位)
- 学生端2: 192.168.3.150 (Win10 32位)
- 频道: 1
- 抓包文件: 5个pcapng (登录、实时查看、屏幕广播、allowMonitor关闭、只看登录)

## 一、UNKNOWN包分析

### 1. CNQT包 (0x434E5154)
- **结论**: 在真实教师端的5个抓包文件中**均未出现**
- **之前观察**: 只在teacher_sim模拟环境中，未登录学生在主端口(4705)发送，32字节
- **推测**: 可能是学生端在特定条件下（如找不到教师端、网络异常）发送的探测包
- **处理建议**: 无需特殊处理，teacher_sim中已添加名称识别即可

### 2. LOGO包 (0x4F474F4C)
- **结论**: 在真实教师端的5个抓包文件中**均未出现**
- **之前观察**: 只在teacher_sim模拟环境中，未登录学生在会话端口发送，8字节内容"LOGO"+0x00000001
- **推测**: 可能是学生端品牌/版本标识包，在真实教师端环境下不发送或被忽略
- **处理建议**: 无需特殊处理

## 二、登录流程（已验证）

```
教师端广播 OONC(0x4F4F4E43) + CANC(0x43414E43) 到 224.50.50.42:4705 (每秒1次)
    ↓
学生端回复 KACA(0x4B414341) 到教师端:4705 (40字节，含学生IP)
    ↓
教师端回复 WACA(0x57414341) (44字节，含教师IP+频道)
    ↓
教师端发送 LPNT(0x4C504E54) subtype=2 + subtype=3 (56字节)
    ↓
教师端发送 DMOC(0x444D4F43) (102字节，含屏幕参数)
    ↓
学生端回复 TRMC(0x54524D43) (40字节)
    ↓
学生端发送 NIPQ(0x4E495051) 到 4809端口 (51字节，持续每秒1次)
    ↓
学生端发送 LANT(0x4C414E54) 缩略图 (JPEG格式，ffd8开头)
```

### 关键包结构

**OONC包 (52字节)**:
```
magic: 0x4F4F4E43 ("OONC")
version: 0x00010000
payload_len: 0x10 (16字节)
hash: 196d6af9295bb946ab958a143ecddc26
teacher_ip: c0a80360 (192.168.3.96)
channel: 0x00000001
sequence: 递增计数器 (01,02,03...)
padding: 00000000
```

**CANC包 (120字节)**:
```
magic: 0x43414E43 ("CANC")
version: 0x00010000
payload_len: 0x54 (84字节)
hash: 196d6af9295bb946ab958a143ecddc26
... 教师名称(Unicode) + 教师IP + 频道 + 动态哈希 ...
```

**KACA包 (40字节)**:
```
magic: 0x4B414341 ("KACA")
version: 0x00010000
payload_len: 0x04 (4字节)
hash: 196d6af9295bb946ab958a143ecddc26
student_ip: c0a80394 (192.168.3.148)
```

**WACA包 (44字节)**:
```
magic: 0x57414341 ("WACA")
version: 0x00010000
payload_len: 0x08 (8字节)
hash: 196d6af9295bb946ab958a143ecddc26
teacher_ip: c0a80360 (192.168.3.96)
channel: 0x00000001
```

## 三、实时查看屏幕 / 屏幕广播流程（重要发现）

**实时查看和屏幕广播使用完全相同的协议！**

```
教师端发送 LPNT(enabled=1, subtype=3) + DMOC
    ↓
学生端发送 TRMC (心跳，持续)
    ↓
学生端发送 DENT(0x544E4544) 帧头 (48字节，含frame_seq)
    ↓
教师端回复 SRNT(0x544E5253) 确认/重传请求 (50字节)
    ↓
学生端发送 LANT(0x4C414E54) 屏幕数据 (JPEG分片，每片约1024字节)
    ↓
    [循环] DENT → SRNT → LANT (持续传输屏幕帧)
```

### DENT包结构 (48字节)
```
magic: 0x544E4544 ("DENT")
version: 0x00010000
payload_len: 0x0C (12字节)
hash: aa3a8dbe2b906645908ea29526218540
student_ip: c0a80396 (192.168.3.150)
student_ip2: c0a80396 (重复)
frame_seq: 0x1E000000 (帧序号，递增)
```

### SRNT包结构 (50字节)
```
magic: 0x544E5253 ("SRNT")
version: 0x00010000
payload_len: 0x0E (14字节)
hash: aa3a8dbe2b906645908ea29526218540
frame_seq: 0x1E000000 (对应DENT的帧序号)
complete: 0x01000000 (1=完整接收, 0=需要重传)
missing_mask: 0x00000000 (缺失分片位图)
padding: 0100
```

### LANT包结构 (可变长度)
```
magic: 0x4C414E54 ("LANT")
version: 0x00010000
payload_len: 0x1504 (548字节，或0x3C=60字节为尾包)
hash: aa3a8dbe2b906645908ea29526218540
student_ip: c0a80394
frame_seq: 0x12000000
width: 0x4A080000 (小端，0x084A=2122? 实际是分辨率相关)
part_index: 0x00000000 (分片索引)
total_parts: 0x04000000 (总分片数)
data: JPEG数据 (ffd8ffdb... 开头)
```

**注意**: LANT包的JPEG数据以`ffd8`开头，是标准JPEG格式，分辨率约320x240或640x480。

## 四、NIPQ包 (4809端口)

- **发送方**: 学生端 → 教师端:4809
- **频率**: 约每秒1次，持续不断
- **长度**: 51字节
- **结构**:
```
magic: 0x4E495051 ("NIPQ")
version: 0x00010000
payload_len: 0x0F (15字节)
hash: 69d7056b304e484bb061ce587b652359
student_ip: c0a80394
... 12字节未知数据 ...
```
- **作用推测**: 网络状态/心跳包，教师端不需要回复
- **teacher_sim处理**: 已正确实现，监听4809端口但不回复

## 五、对截图替换功能的意义

### 协议层面的截图替换原理
学生端发送屏幕数据的完整链路：
1. 学生端调用GDI函数(CreateDCW/BitBlt)捕获屏幕
2. 学生端将屏幕截图编码为JPEG
3. 学生端通过DENT/LANT包发送JPEG数据

### 两种截图替换方案

**方案A: Hook GDI函数（原作者方案，已禁用）**
- Hook CreateDCW/BitBlt，返回假的屏幕DC
- 优点: 在捕获源头替换，所有屏幕相关功能都生效
- 缺点: 32位Win10驱动兼容性问题，关闭allowMonitor时崩溃
- 当前状态: 已禁用

**方案B: Hook网络发送（新方案，基于抓包分析）**
- Hook学生端的socket发送函数，在DENT/LANT包发送前替换JPEG数据
- 优点: 不涉及GDI/驱动，兼容性好
- 缺点: 需要解析和修改网络包，实现复杂
- 可行性: 抓包已完全解析DENT/LANT包结构，可以实现

### 建议
1. 短期: 保持截图替换禁用，添加警告告示（已完成）
2. 中期: 评估方案B的可行性，基于DENT/LANT包结构实现网络层替换
3. 长期: 如果方案B可行，重新启用截图替换功能

## 六、teacher_sim协议完善建议

### 已正确实现
- ✅ OONC/CANC广播接收
- ✅ KACA→WACA响应
- ✅ LPNT+DMOC发送
- ✅ TRMC接收和响应
- ✅ DENT接收和SRNT响应
- ✅ LANT接收和JPEG重组保存
- ✅ NIPQ 4809端口监听（不回复）

### 待完善
- ⚠️ CNQT/LOGO包: 真实环境不出现，无需处理
- ⚠️ 屏幕广播: teacher_sim作为教师端发起广播的功能尚未实现（当前只能接收学生屏幕）
- ⚠️ 全体命令: 已实现关机/重启，其他命令可扩展

## 七、结论

1. **UNKNOWN包(CNQT/LOGO)在真实教师端环境中不存在**，只在teacher_sim模拟环境出现，无需特殊处理
2. **实时查看和屏幕广播使用相同协议**（DENT/SRNT/LANT），抓包已完全解析
3. **截图替换可以考虑网络层方案**（Hook socket发送替换LANT包的JPEG数据），避免GDI/驱动兼容性问题
4. **teacher_sim的6.0协议实现基本正确**，核心功能（登录、查看学生屏幕、发送命令）都能正常工作

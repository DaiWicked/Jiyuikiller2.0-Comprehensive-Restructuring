# 对《监控画面替换 - DLL源码分析请求》的回复

先说结论，然后是能给出的技术核对结果。

## 一、这个任务的哪一部分我不做

**要做的**："让截图替换在 `allowMonitor=true`（教师端显示为"允许监视"）时也生效"——即让被监视方看到的是伪造画面，同时让监视方认为监视是正常放行的。

**我不做这一部分**，包括：DLL 修改点（第 68 题 Q3）、"只改判断条件"的具体改法、以及 Q7 里"换一种方式实现同一效果"的替代路径。

原因与"设备归谁/是否授权"无关——你的实验室（32 位 VM 学生端 + 宿主教师端）我接受，前面的窗口控制、驱动协议那些工作我也一直在做。区别在于：前面那些改动影响的是**你机器上的软件行为**，而这个改动唯一的作用是**向监视方提供一份设计成无法被觉察的伪造画面**。原作者的 DLL 里把假屏与 `allowMonitor=false` 绑在一起，是一个刻意设计——"拒绝被监视"这件事对教师端是**可见**的（极域会显示监视失败）。把它解耦，等于新增一个上游作者明确没有提供的"监视照常、内容是假的"能力，而且这个能力一旦编译出来就不局限在你的虚拟机里（它是一个注入到任何极域学生端的 DLL）。这条线我停在这里。

顺带说明：**这个任务在技术上也基本没有"安全改法"**——原因见第四节，这是我在核对源码后得出的事实判断，不是借口。

## 二、提示词里有 4 处技术前提是错的（先纠正，能省掉更多死机）

### 2.1 DLL 里**没有** `hkBitBlt` / `hkStretchBlt`

我 grep 了你实际编译的那份源码（`工程文件\JiYuKiller\JiYuTrainerHooks\JiYuTrainerHooks.cpp`）：**不存在任何 BitBlt/StretchBlt 钩子**。

所以"之前的失败经验"里第 2 条（"在 `hkBitBlt` 中拦截全屏并用 GDI+ DrawImage 替换"）是**自己新增了一个原本不存在的钩子**。而 GDI+ 的 `Graphics::DrawImage` 内部会再走 `StretchBlt/BitBlt` → 触发你自己的钩子 → **无限递归，栈溢出**。这是必然结果，不是"运气不好"。同理第 3 条"仅添加诊断钩子"——任何新钩子都要用 mhook 改写目标函数的开头字节，装在这些被高频调用的 GDI 函数上，风险极高。

### 2.2 截图替换不在"编码/传输"环节，而在**采集入口**

假屏机制真正用到的函数是这些（行号为你那份源码）：

| 行号 | 函数 | 作用 |
|---|---|---|
| `:1691-1696` | `hkGetDesktopWindow` | `loaded` 时：`return allowMonitor ? desktopWindow : fakeDesktopWindow;` ← **总开关** |
| `:1697-1701` | `hkGetWindowDC` | `hWnd==desktopWindow && !allowMonitor` → 返回假桌面窗口的 DC |
| `:1702-1706` | `hkEncodeToJPEGBuffer` | `if (!allowMonitor) return FALSE;` ← 直接掐断编码 |
| `:1711-1721` | `hkCreateDCW` | `!allowMonitor && driver=="DISPLAY"` → **返回 NULL** |
| `:1592` | `hkDwmSetWindowAttribute` 附近 | `!allowMonitor && DWM_EC_DISABLECOMPOSITION` → 阻止关闭 DWM 合成 |
| `:1766-1771` | `FakeDesktopWndProc` 的 `WM_PAINT` | **`g_fakeScreenImage` 全 DLL 唯一的使用点**：把图片画到假桌面窗口上 |

### 2.3 `g_fakeScreenImage` 不是"替换器"，它只是"假桌面窗口画什么"

`:92` 定义、`:177` 销毁、`:357-363` 从 INI 加载、`:1766-1771` 绘制。**全文件只有这 4 处**。

也就是说：图片本身不参与任何采集/编码替换；它只决定那个假桌面窗口显示什么（**没设图片时它画纯黑**，`:1773-1780`）。真正的"替换"动作是 `allowMonitor=false` 导致的 **DC 重定向 + 编码函数直接返回 FALSE**。

→ 因此提示词第 70 行设想的代码 `if (allowMonitor && g_fakeScreenImage)` **在整个 DLL 里不存在**，没有"改成 `if (g_fakeScreenImage)`"这处可改。

### 2.4 `faGetWindowDC` 那行确实是上游 bug，而且比想象的严重

```
:1046  faGetDesktopWindow = (fnGetDesktopWindow)GetProcAddress(hUser32, "GetDesktopWindow");
:1047  faGetWindowDC      = (fnGetWindowDC)     GetProcAddress(hUser32, "GetDesktopWindow");   // ← 取的是 GetDesktopWindow
...
:1116  hk26 = Mhook_SetHook((PVOID*)&faGetWindowDC,      hkGetWindowDC);
:1117  hk27 = Mhook_SetHook((PVOID*)&faGetDesktopWindow, hkGetDesktopWindow);
```

`faGetWindowDC` 被赋成了 **GetDesktopWindow 的地址**。于是：

1. `:1116` 实际是**在 `GetDesktopWindow` 上装钩子**（而不是 GetWindowDC）；
2. `:1117` 又在 `GetDesktopWindow` 上装**第二个**钩子 → **同一个目标函数被 mhook 装了两次 inline hook**，跳板字节被二次改写 → 典型的未定义行为/崩溃源；
3. `hkGetWindowDC` 的兜底分支 `return faGetWindowDC(hWnd);`（`:1700`）会把 `GetDesktopWindow()` 的返回值（HWND）当成 HDC 用 → 类型错用。

`GetDesktopWindow` 是极高频率调用（每次菜单/提示/窗口创建都会走）。这条 bug **不是你们改坏的，是上游的**——`JiYuTrainer_Next-master\JiYuTrainerHooks\JiYuTrainerHooks.cpp:1047` 一模一样。

这也解释了你们"仅修复 faGetWindowDC 笔误 → 死机"：修好之后 `hkGetWindowDC` 才真正挂到 GetWindowDC 上（覆盖面急剧扩大），而那两处重复钩子的问题依然存在 → 更容易踩雷。

## 三、Q4：原作者项目的实现与你们的 DLL 有什么区别？

**没有区别——是同一套机制。** 逐点比对（`JiYuTrainer_Next-master` 对应行号）：

| 关键点 | 原作者项目 | 你们的 DLL | 是否相同 |
|---|---|---|---|
| 假图全局变量 | `:92-93` | `:92-93` | ✅ 相同 |
| 从 INI 读 `FakeScreenImage` | `:357-364` | `:357-364` | ✅ 相同 |
| `hkGetDesktopWindow` 的 `allowMonitor` 三元 | `:1691-1696` | `:1691-1696` | ✅ 相同 |
| `hkGetWindowDC` 重定向 | `:1697-1701` | `:1697-1701` | ✅ 相同 |
| `hkEncodeToJPEGBuffer` 直接返回 FALSE | `:1702-1706` | `:1702-1706` | ✅ 相同 |
| `hkCreateDCW` 返回 NULL | `:1711-1721` | `:1711-1721` | ✅ 相同 |
| 图片唯一使用点 | `:1766-1771` | `:1766-1771` | ✅ 相同 |
| `faGetWindowDC` 取错导出名 | `:1047` | `:1047` | ✅ 相同（同一个 bug） |
| `hkBitBlt`/`hkStretchBlt` | 不存在 | 不存在 | ✅ 相同 |

所以"对照原作者项目寻找另一种实现"这条路是走不通的：两版是同一份代码。原作者从未提供"监视照常 + 内容是假的"这种模式。

## 四、Q1/Q2/Q5/Q6：调用链与崩溃机理（这部分是安全分析，我给）

### 4.1 完整调用链（教师点"查看学生屏幕"之后）

```
教师端请求监视
   ↓
学生端 StudentMain 准备采集本机屏幕
   ↓
① 取桌面窗口句柄      GetDesktopWindow()  ──hk27──→ hkGetDesktopWindow      (:1691-1696)
② 取桌面 DC           GetWindowDC(hwnd)   ──hk26──→ hkGetWindowDC           (:1697-1701)
③ 取显示设备 DC       CreateDCW(DISPLAY)  ────────→ hkCreateDCW             (:1711-1721)
④ 抓图 → 编码 JPEG    EncodeToJPEGBuffer   ──hk28──→ hkEncodeToJPEGBuffer    (:1702-1706)
   ↓
⑤ 通过 UDP/UMSP 通道发给教师端
```

DLL 的介入点只有 ①②③④ 四个入口，全部以 `allowMonitor` 为唯一开关。**图片（`g_fakeScreenImage`）不在这条链上**，它只出现在假桌面窗口的绘制里（`:1766-1771`），而假桌面窗口只有在 ① 返回它之后才会被采集到。

### 4.2 `allowMonitor` 与 `g_fakeScreenImage` 的关系（Q2）

- `allowMonitor` = **开关**：决定"采集到的是真实桌面，还是那个屏幕外的假桌面窗口"；
- `g_fakeScreenImage` = **内容**：决定"假桌面上画什么"（无图 → 纯黑）。

两者是"开关/内容"的从属关系，不是并列条件。所以不存在"解耦一下判断条件就行"的小改动——**"替换"这件事本身就是由 `allowMonitor=false` 实现的**。

### 4.3 为什么关闭 `allowMonitor` 会让极域崩溃（Q5）

提示词把它归因于"32 位 Win10 驱动兼容性"，我认为更可能是下面两条（可直接用日志/调试器验证，不需要改代码）：

1. **`hkCreateDCW` 返回 NULL（`:1711-1720`）**：这是官方"禁止监视"的实现方式。极域拿到 NULL HDC 后如果不判空就继续 `BitBlt/SelectObject` → 访问违例崩溃。**这解释了"教师一尝试查看就崩"**——崩溃点是采集路径，而不是驱动。
2. **`faGetWindowDC` 取错导出名（`:1047`）+ 双钩子（`:1116/1117`）**：采集路径恰好会大量调用 `GetDesktopWindow`，而该地址上被装了两次 inline hook；`hkGetWindowDC` 的兜底分支还会把 HWND 当 HDC 返回。这两点都会在采集时被触发。

两者的共同点：**崩溃发生在 `allowMonitor=false` 特有的分支里**。所以——

### 4.4 Q3「只改判断条件」为什么不可行（技术上就走不通）

提示词里设想的"只改判断条件、不碰钩子主体"有两条可能路径，**两条都不安全**：

- **路径 A：让假屏分支在 `allowMonitor=true` 时也执行** → 等价于把 `allowMonitor` 当成 false 用 → **必然走进 `hkCreateDCW→NULL`、`hkEncodeToJPEGBuffer→FALSE` 这些分支，也就是必然复现同一个崩溃**（你们"失败经验"第 5 条"仅修改 allowMonitor 判断逻辑 → 死机"正是这个原因，而不是巧合）。
- **路径 B：不动这些分支，改成在采集/编码热路径上把数据换成图片** → 那就是失败经验第 2 条（自己加 BitBlt 钩子）——而 GDI+ 内部又会调用 BitBlt/StretchBlt，导致重入递归、栈溢出死机。

**结论（Q6 风险评估）**：这个目标没有"低风险改法"。原因是假屏机制**本身**就建立在"让采集路径失败"之上（返回 NULL / 返回 FALSE），而你们要的是"采集路径成功、但内容是假的"——这是与上游设计相反的语义，必须改写采集或编码的主体逻辑，也就是那 5 次死机所在的位置。风险不是"谨慎一点就能规避"，而是**结构性**的。

## 五、我建议的方向（不是绕过上述结论的替代方案）

1. **接受上游设计**：`allowMonitor=false` 就是"假屏 + 拒绝监视"一体；`allowMonitor=true` 就是"真屏 + 允许监视"。中间态在上游不存在，也不该由我们补。
2. **把这次的崩溃当成上游 bug 报出去**（这个我可以帮你把材料写全，且对所有人有用）：
   - `:1047` 取错导出名 → 导致 `GetDesktopWindow` 被重复 inline hook；
   - `:1711-1720` 返回 NULL / `:1702-1706` 返回 FALSE 时调用方无判空保护；
   - `:1700` HWND/HDC 类型混用。
   这是一份标准的可复现缺陷报告，交给 JiYuTrainer 原作者或上游仓库。
3. **实验室稳定性与工程卫生**（与是否实现某功能无关）：
   - `JiYuTrainerHooks\` 目录里现在同时有 `JiYuTrainerHooks.cpp`、`.modified`、`.with_stretchdbg`、`.bak_before_move_fix` 四份源码——注入用的 DLL 必须能对应到唯一一份可复现源码，建议回到 `.bak_before_move_fix` 基线、把实验文件移出构建目录（否则以后没人能确定线上 DLL 里到底编了什么）。
   - 测试前给 32 位 VM 打快照（死机后可秒回滚），教师端别开自动启动注入。
4. **继续推进我们已经确认的 C# 侧修复**（这些与假屏无关，且已定位）：窗口控制回调接线、`_gbFullManual`、`AllowGbTop` 改被动、`AutoIncludeFullWindow`、驱动 `CTL_INITPARAM` 4 条、设置持久化等。

## 六、我能继续帮的清单

- C# 侧全部已定位问题的补丁（窗口控制 / 驱动协议 / 设置 / UI）。
- 上述 DLL 上游缺陷的**可复现报告**撰写（给上游，不是你本地热改）。
- 实验室工程卫生与测试流程（快照、基线、发布 DLL 与源码对应）。
- 许可与署名补齐。

不能帮的：让"监视照常显示、内容是伪造且不可觉察"这件事生效——不论是在 DLL 里改判断、改采集路径，还是改用其它手段实现同一效果。

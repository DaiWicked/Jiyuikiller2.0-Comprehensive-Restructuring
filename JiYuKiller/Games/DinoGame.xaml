<UserControl x:Class="JiYuKiller.Games.DinoGame"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="440"
             Focusable="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="← 返回" Click="Back_Click" Style="{StaticResource GlassButton}" Width="70" Height="28" FontSize="12" Padding="0" Margin="0,0,10,0"/>
            <TextBlock Text="恐龙跳" FontSize="16" FontWeight="Bold" Foreground="#FF1a1a2e" VerticalAlignment="Center" Margin="0,0,16,0"/>
            <Border Background="#15000000" CornerRadius="6" Padding="10,4" VerticalAlignment="Center">
                <TextBlock x:Name="ScoreText" Text="分数: 0" FontSize="13" Foreground="#FF1a1a2e" VerticalAlignment="Center"/>
            </Border>
            <Border Background="#15000000" CornerRadius="6" Padding="10,4" Margin="8,0,0,0" VerticalAlignment="Center">
                <TextBlock x:Name="SpeedText" Text="速度: 1.0x" FontSize="13" Foreground="#FF1a1a2e" VerticalAlignment="Center"/>
            </Border>
            <Button Content="重新开始" Click="Restart_Click" Style="{StaticResource GlassButton}" Width="70" Height="28" FontSize="12" Padding="0" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- 游戏区域：全部图形由代码用 WPF 形状绘制，ClipToBounds 防止仙人掌在画布外可见 -->
        <Border Grid.Row="1" Background="#FFF7F7F7" CornerRadius="8" Padding="4" HorizontalAlignment="Center" VerticalAlignment="Center" MouseLeftButtonDown="GameCanvas_Click">
            <Canvas x:Name="GameCanvas" Width="360" Height="180" Background="#FFF7F7F7" ClipToBounds="True"/>
        </Border>

        <TextBlock Grid.Row="2" Text="空格/点击跳跃，躲避仙人掌" FontSize="11" Foreground="#FF888899" Margin="0,10,0,0" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>

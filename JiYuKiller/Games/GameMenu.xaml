<UserControl x:Class="JiYuKiller.Games.GameMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="选择游戏" FontSize="16" FontWeight="Bold" Foreground="#FF1a1a2e" Margin="0,0,0,12"/>
        <WrapPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <!-- 扫雷卡片 -->
            <Border Background="#15000000" CornerRadius="12" Padding="0" Margin="0,0,12,12" Width="150" Height="170" MouseLeftButtonUp="Minesweeper_Click" Cursor="Hand">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.15"/>
                </Border.Effect>
                <StackPanel>
                    <Border Background="#FFDC3545" CornerRadius="12,12,0,0" Height="80">
                        <Viewbox Margin="20">
                            <TextBlock Text="💣" FontSize="48"/>
                        </Viewbox>
                    </Border>
                    <StackPanel Margin="12,10">
                        <TextBlock Text="扫雷" FontSize="16" FontWeight="Bold" Foreground="#FF1a1a2e"/>
                        <TextBlock Text="左键挖雷，右键标旗" FontSize="11" Foreground="#FF888899" Margin="0,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            <!-- 恐龙跳卡片 -->
            <Border Background="#15000000" CornerRadius="12" Padding="0" Margin="0,0,12,12" Width="150" Height="170" MouseLeftButtonUp="Dino_Click" Cursor="Hand">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.15"/>
                </Border.Effect>
                <StackPanel>
                    <Border Background="#FF535353" CornerRadius="12,12,0,0" Height="80">
                        <Viewbox Margin="20">
                            <TextBlock Text="🦖" FontSize="48"/>
                        </Viewbox>
                    </Border>
                    <StackPanel Margin="12,10">
                        <TextBlock Text="恐龙跳" FontSize="16" FontWeight="Bold" Foreground="#FF1a1a2e"/>
                        <TextBlock Text="空格/点击跳跃，躲避仙人掌" FontSize="11" Foreground="#FF888899" Margin="0,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </WrapPanel>
    </Grid>
</UserControl>

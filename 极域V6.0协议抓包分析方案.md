# 极域V6.0协议抓包分析方案

## 目标
1. 分析UNKNOWN包（CNQT/LOGO）的完整内容和教师端响应
2. 分析教师端实时查看学生屏幕的完整协议流程
3. 为后期重新启用截图替换功能提供协议依据

---

## 一、环境准备

### 1.1 硬件/软件
- **教师端**：真实极域教师端 `TeacherMain.exe`（V6.0 2016豪华版）
  - 路径：`C:\Program Files (x86)\Mythware\极域课堂管理系统软件V6.0 2016 豪华版\TeacherMain.exe`
- **学生端**：极域学生端 `StudentMain.exe`（V6.0）
  - 虚拟机：Win10 32位 / Win7 32位
- **抓包工具**：Wireshark
- **网络**：VMware桥接模式（vmnet0），教师端和学生端在同一网段

### 1.2 Wireshark配置
- **捕获网卡**：选择VMware桥接对应的物理网卡（不是VMnet1/VMnet8）
- **捕获过滤器**：`udp portrange 4700-4900 or tcp portrange 4700-4900`
  - 4705：主端口（广播/命令）
  - 4805：会话端口（MESS消息）
  - 4806：屏幕观看端口（TCP/UMSP）
  - 4809：NIPQ端口
- **显示过滤器**：`udp or tcp` （先全抓，事后过滤）

### 1.3 抓包前准备
1. 关闭学习不通主程序（避免干扰）
2. 确保学生端未被注入DLL
3. 教师端和学生端都启动，但**不要登录**
4. 开始抓包

---

## 二、抓包步骤（分阶段）

### 阶段1：登录握手（重点：CNQT/LOGO包）

**操作**：
1. 开始抓包
2. 学生端启动，自动登录教师端
3. 等待登录完成（学生端出现在教师端列表中）
4. 停止抓包，保存为 `v6_login_full.pcapng`

**重点关注**：
- CNQT包的完整32字节内容
- LOGO包的发送时机和内容
- 教师端对这两个包是否有响应
- 完整的登录握手流程（KACA→WACA→...→登录成功）

**过滤命令**：
```
# 查看CNQT包
udp contains 54:51:4e:43

# 查看LOGO包
udp contains 4c:4f:47:4f

# 查看登录流程
udp.port == 4705 or udp.port == 4805
```

---

### 阶段2：教师端实时查看学生屏幕（重点：截图替换）

**操作**：
1. 学生端已登录
2. 开始抓包
3. 教师端点击"查看学生屏幕"（单个学生）
4. 观察教师端界面，确认能看到学生屏幕
5. 持续10-15秒
6. 教师端停止查看
7. 停止抓包，保存为 `v6_view_screen.pcapng`

**重点关注**：
- 教师端发送什么包触发学生端开始发送屏幕数据
- 屏幕数据的传输协议（TCP 4806? UDP?）
- 屏幕数据的格式（JPEG? 原始位图? 分片?）
- 学生端发送屏幕数据的频率
- 教师端如何停止接收

**过滤命令**：
```
# 屏幕观看端口
tcp.port == 4806 or udp.port == 4806

# 查看LPNT/DMOC/TRMC相关包
udp contains 4c:50:4e:54 or udp contains 44:4d:4f:43 or udp contains 54:52:4d:43

# 查看LANT（缩略图）包
udp contains 4c:41:4e:54
```

---

### 阶段3：allowMonitor关闭时的行为（重点：崩溃原因）

**操作**：
1. 学生端已登录
2. **在学生端设置allowMonitor=False**（通过学习不通或注册表）
3. 开始抓包
4. 教师端点击"查看学生屏幕"
5. 观察学生端是否崩溃
6. 如果崩溃，记录崩溃前的最后几个包
7. 停止抓包，保存为 `v6_allowmonitor_off.pcapng`

**重点关注**：
- allowMonitor关闭后，学生端对教师端查看请求的响应
- 崩溃前最后收到的包
- 教师端是否重发请求
- 对比阶段2的正常流程，找出差异

**注意**：此步骤可能导致学生端崩溃，建议先拍快照

---

### 阶段4：教师端广播屏幕（对比验证）

**操作**：
1. 学生端已登录
2. 开始抓包
3. 教师端发起"屏幕广播"
4. 持续10秒
5. 停止广播
6. 停止抓包，保存为 `v6_broadcast.pcapng`

**重点关注**：
- 广播和查看学生屏幕的协议差异
- 广播数据的流向（教师→学生）

---

## 三、数据包分析要点

### 3.1 CNQT包分析
- 32字节完整内容
- 字段结构推测：magic(4) + version(4) + ?(24)
- 是否包含学生端IP/MAC/版本信息
- 教师端是否回复

### 3.2 LOGO包分析
- 8字节：magic(4) + u32(1)
- u32字段的含义（版本号? 标志位?）
- 发送时机（登录前? 登录后?）
- 教师端是否回复

### 3.3 实时查看屏幕流程
```
教师端                        学生端
  |                            |
  |-- [请求查看屏幕] --------->|
  |                            |-- 开始编码屏幕
  |<-- [屏幕数据分片] ---------|
  |<-- [屏幕数据分片] ---------|
  |<-- [屏幕数据分片] ---------|
  |                            |
  |-- [停止查看] ------------->|
  |                            |-- 停止编码
```

需要确认：
1. 请求查看屏幕的包类型和内容
2. 屏幕数据的端口和协议
3. 分片重组方式
4. 停止查看的包类型

### 3.4 截图替换的关键
截图替换的核心是：**在学生端发送屏幕数据给教师端之前，替换屏幕数据**

需要确认：
1. 屏幕数据是在学生端哪个模块编码的
2. 是否可以通过DLL注入拦截编码前的截图API
3. 替换后的数据格式是否需要和原数据一致
4. 教师端对屏幕数据的校验机制（如果有）

---

## 四、预期成果

### 4.1 协议文档
- CNQT包结构和作用
- LOGO包结构和作用
- 实时查看屏幕的完整协议流程
- 屏幕数据格式

### 4.2 截图替换可行性评估
- 方案A：DLL注入拦截截图API（原作者方案，已知有bug）
- 方案B：网络层拦截替换（需要了解屏幕数据格式）
- 方案C：修改学生端配置（如果有相关设置）
- 各方案的优缺点和风险

### 4.3 teacher_sim完善
- 添加CNQT/LOGO包的正确响应（如果需要）
- 完善实时查看学生屏幕功能（当前只有preview缩略图）

---

## 五、注意事项

1. **先拍虚拟机快照**：阶段3可能导致学生端崩溃
2. **多次抓包**：每个阶段至少抓2次，确认一致性
3. **记录时间点**：在抓包时记录关键操作的时间点，便于事后定位
4. **对比分析**：将真实教师端的抓包和teacher_sim的日志对比，找出差异
5. **保存原始包**：pcapng文件保留，后续可反复分析

---

## 六、后续工作

1. 根据抓包结果完善teacher_sim协议
2. 评估截图替换的可行方案
3. 如果方案可行，实现并测试
4. 如果不可行，保持截图替换禁用状态

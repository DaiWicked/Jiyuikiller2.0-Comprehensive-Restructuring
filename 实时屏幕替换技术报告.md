# 极域电子教室实时屏幕替换技术报告

## 1. 项目背景

极域电子教室（Mythware）是一款广泛使用的教学管理软件，教师端可以实时查看学生端屏幕。本项目旨在通过逆向工程，实现对学生端实时编码画面的替换，使教师端看到的不是真实屏幕，而是预设的假画面。

### 1.1 目标
- 在学生端运行时，拦截实时屏幕编码流程
- 将编码输入的YUV帧替换为自定义画面
- 教师端看到替换后的画面，学生端无感知
- 多次切换观看目标不崩溃

### 1.2 环境
- 学生端：极域课堂管理系统软件V6.0 2016豪华版（32位）
- 测试系统：Windows 7 32位 / Windows 10 32位（虚拟机）
- 开发工具：Visual Studio 2022（Win32 Release）
- 注入方式：DLL注入 + Mhook/vtable patch

---

## 2. 逆向过程

### 2.1 方案一：网络层I帧替换（已排除）

**思路**：拦截学生端发往教师端的H.264网络流，替换I帧数据。

**实现**：开发 `realtime_analyzer.py`，连接学生端4806端口接收H.264流，分析帧结构。

**发现**：
- 极域编码器策略：首帧I帧 + 后续全P帧
- **无周期性I帧**（关键帧间隔=25，但实际只在开始时发一个I帧）
- P帧依赖前一帧，无法单独替换

**结论**：网络层I帧替换不可行。

### 2.2 方案二：avcodec_encode_video hook（已排除）

**思路**：极域使用FFmpeg的LibAVCodec52.dll，hook `avcodec_encode_video`。

**发现**：`avcodec_encode_video` 存在但**实时编码时不调用**（调用次数为0）。

**结论**：不是实时编码入口。

### 2.3 方案三：定位真实编码入口（成功）

#### 2.3.1 分析libTDDesk2.dll导入表

使用 `dumpbin /imports` 发现极域核心模块导入的是：

```
LibVOIP10.dll!IH264EncoderModule::CreateCBREncoder
  完整修饰名：?CreateCBREncoder@IH264EncoderModule@VoIP@@SAHKHHMHHHHHHHAAPAVIVideoEncoderModule@2@@Z
  ordinal: 71

LibVOIP10.dll!IH264EncoderModule::CreateVBREncoder
  ordinal: 74
```

#### 2.3.2 CreateCBREncoder参数

| 参数 | 值 | 含义 |
|------|-----|------|
| a1 | 0x30323449 | FourCC = "I420"（YUV420格式） |
| a2 | 动态 | 编码宽度（见2.3.3） |
| a3 | 动态 | 编码高度 |
| a4 | 25.0 | 帧率(fps) |
| a5 | 100 | 未知 |
| a6 | 20480 | 码率(kbps) |
| a7 | 25 | 关键帧间隔 |
| a8 | 25 | 未知 |
| a9 | 4000 | 未知 |
| a10 | 1 | 未知 |
| a11 | 0 | 未知 |
| a12 | outEncoder | 输出编码器接口指针 |

#### 2.3.3 编码尺寸（动态，重要！）

编码尺寸**不是固定的**，取决于学生端屏幕分辨率：

| 测试环境 | 编码宽度 | 编码高度 | YUV大小 |
|----------|----------|----------|---------|
| Win7 VM (1024×768) | 1024 | 768 | 1,179,648 |
| Win7 VM (其他分辨率) | 1024 | 784 | 1,204,224 |
| 另一台测试机 | 960 | 672 | 967,680 |

**结论**：必须从CreateCBREncoder参数动态获取宽高，不能硬编码。

#### 2.3.4 IVideoEncoderModule虚函数表分析

`CreateCBREncoder` 返回 `IVideoEncoderModule*` 接口。通过dump虚函数表（24个函数）+ `dumpbin /disasm` 反汇编：

| 索引 | RVA | 特征 | 功能 |
|------|-----|------|------|
| [0] | 0x42C60 | thunk | 析构函数 |
| [5] | 0x3D0F0 | 6条指令 | getter |
| [6] | 0x34160 | ret 4 | setter |
| **[9]** | **0x42B80** | **SEH帧，ret 10h(4参数)，调用内部编码函数，帧/字节计数器递增** | **Encode方法** |

### 2.4 方案四：vtable patch（成功）

#### 2.4.1 为什么不用Mhook直接hook？

Mhook hook虚函数地址会崩溃——虚函数是`__thiscall`，Mhook的trampoline不处理`ecx`(this指针)。

#### 2.4.2 vtable patch方案

```cpp
void **vtable = *(void***)outEncoder;
DWORD oldProtect;
VirtualProtect(&vtable[9], 4, PAGE_READWRITE, &oldProtect);
g_origEncode = vtable[9];
vtable[9] = (void*)hkEncodeProbe;
VirtualProtect(&vtable[9], 4, oldProtect, &oldProtect);
```

#### 2.4.3 hook函数（__fastcall + 内联汇编）

```cpp
int __fastcall hkEncodeProbe(void *this_ptr, void *edx,
                              void *arg1, void *arg2,
                              void *arg3, int arg4) {
    // 替换YUV输入帧
    if (g_replaceEnabled && arg1 && g_fakeYUV && g_fakeYUVSize > 0)
        memcpy(arg1, g_fakeYUV, g_fakeYUVSize);

    int encodeRet = 0;
    __asm {
        mov ecx, this_ptr   // 恢复this指针
        push arg4; push arg3; push arg2; push arg1
        mov eax, g_origEncode; call eax
        mov encodeRet, eax
    }
    return encodeRet;
}
```

**关键**：内联汇编手动设置`ecx=this_ptr`，正确传递__thiscall的this指针。

### 2.5 Encode参数逆向（关键突破）

#### arg1 = YUV420输入数据缓冲区（直接可写）

最初误以为arg1是结构体，因为dump arg1[0]读到了字符串。实际分析：
- `ARG1_DUMP` 全是 `0x77777777`（每个字节0x77=灰色Y值）
- 之前把arg1[0]（值0x77777777）当作指针dump，碰巧读到系统内存字符串
- 静止画面时YUV数据全是相同值，符合预期

**确认依据**：
1. CreateCBREncoder的a1="I420"，输入格式是YUV420
2. arg3[6] = arg1（帧描述符中的输入指针字段）
3. memcpy替换后教师端看到假画面（实测验证）

#### arg2 = H.264输出缓冲区

固定地址，编码后的H.264码流写入此处。

#### arg3 = 帧描述符结构体

```
偏移  示例值     含义
[0]   0x400=1024 宽度
[1]   1          标志/帧类型
[2]   0          保留
[3]   0          保留
[4]   0x400=1024 stride（行步长）
[5]   0x300=768  高度
[6]   arg1       输入YUV缓冲区指针
[7]   arg2       输出缓冲区指针
```

#### arg4 = 0x18 = 24

格式标识。

---

## 3. 实时屏幕替换实现

### 3.1 核心原理

在 `hkEncodeProbe` 中，调用原Encode函数**之前**，将arg1指向的YUV数据替换为预设的假画面：

```cpp
if (g_replaceEnabled && arg1 && g_fakeYUV && g_fakeYUVSize > 0)
    memcpy(arg1, g_fakeYUV, g_fakeYUVSize);
```

### 3.2 假画面生成（动态尺寸）

在 `CreateCBREncoder` 返回后，根据实际编码尺寸生成YUV画面：

```cpp
g_encW = a2; g_encH = a3;
g_fakeYUVSize = g_encW * g_encH * 3 / 2;
g_fakeYUV = (BYTE*)malloc(g_fakeYUVSize);

// Y平面：左半红(Y=76)，右半蓝(Y=29)
for (y=0; y<g_encH; y++)
    for (x=0; x<g_encW; x++)
        g_fakeYUV[y*g_encW+x] = (x < g_encW/2) ? 76 : 29;

// U平面(Cb)：左半255，右半85
// V平面(Cr)：左半128，右半255
```

### 3.3 U/V平面交换（重要发现）

测试发现，初始U/V值设置后教师端显示的颜色是**左蓝右红**（与预期左红右蓝相反）。

**原因**：极域编码器的I420格式中，U和V平面的顺序可能与标准I420相反，或者编码器内部对U/V有特殊处理。

**修复**：调整U/V值（U左255右85，V左128右255），教师端正确显示左红右蓝。

### 3.4 调用流程

```
学生端启动 → DLL注入 → Mhook hook CreateCBREncoder
  ↓
教师端发起实时观看
  ↓
hkCreateH264CBREncoder 被调用
  ├─ 记录参数（I420, 动态W×H, 25fps）
  ├─ 调用原函数创建编码器
  ├─ dump虚函数表（24个函数）
  ├─ vtable patch [9] → hkEncodeProbe（防重复）
  └─ 生成假YUV画面（动态尺寸，尺寸不变则复用）
  ↓
编码器开始编码（25fps）
  ↓
每帧调用 IVideoEncoderModule::Encode [9]
  ↓
hkEncodeProbe 被调用
  ├─ memcpy(arg1, g_fakeYUV, g_fakeYUVSize)  ← 替换输入帧
  └─ 内联汇编调用原Encode函数
  ↓
H.264码流发送到教师端 → 教师端显示假画面
```

---

## 4. 踩坑记录与修复

### 4.1 第二次发起观看崩溃（严重）

**现象**：第一次观看正常，停止后第二次发起观看，极域崩溃。

**根因**：
- 虚函数表是**共享的**（多个编码器对象使用同一个vtable）
- 第二次CreateCBREncoder时，vtable[9]已经是hkEncodeProbe
- 代码没有检查，直接 `g_origEncode = vtable[9]`，把g_origEncode覆盖成hook函数自己
- 调用原函数时变成调用自己 → **无限递归 → 栈溢出崩溃**

**修复**：
```cpp
if (vtable[9] != (DWORD)hkEncodeProbe) {
    g_origEncode = (void*)vtable[9];
    vtable[9] = (DWORD)hkEncodeProbe;
    // patch...
} else {
    AvcodecLog("H264_VTABLE_ALREADY_PATCHED, skip");
}
```

### 4.2 假YUV重复free/malloc导致use-after-free

**现象**：第二次创建编码器时free旧g_fakeYUV再malloc新的，如果旧编码器还在运行会崩溃。

**修复**：尺寸相同时不重新分配，直接复用：
```cpp
if (g_encW != a2 || g_encH != a3 || !g_fakeYUV) {
    // 重新分配
} else {
    AvcodecLog("FAKE_YUV reuse");
}
```

### 4.3 硬编码尺寸导致替换不生效

**现象**：最初硬编码960×672，但实际编码尺寸是1024×768，尺寸校验不通过，替换不执行。

**修复**：从CreateCBREncoder参数动态获取宽高。

### 4.4 C++路径陷阱

文件路径字符串中 `\a` 被解释为响铃符(0x07)，导致日志文件永远创建失败。必须用 `\\` 双反斜杠。

---

## 5. 测试结果

### 5.1 稳定性
- vtable patch后极域学生端**不崩溃**
- Encode函数被调用2600+次（约104秒@25fps）
- **多次切换观看目标稳定**（防重复patch生效）
- 虚函数表共享，patch一次永久生效

### 5.2 替换效果（实测成功）
- 测试画面：左半红色、右半蓝色的YUV画面
- 教师端看到：**左右分色的静止画面**（左红右蓝）
- 学生端无感知，正常运行
- U/V平面顺序已调整正确

### 5.3 参数确认
- 编码格式：I420（YUV420 Planar）
- 编码尺寸：动态（1024×768 / 1024×784 / 960×672）
- 帧率：25fps，码率：20480kbps
- arg1直接指向YUV数据缓冲区（可写）

---

## 6. 关键技术点总结

### 6.1 已排除的死路
1. ❌ 网络层I帧替换：无周期性I帧
2. ❌ avcodec_encode_video hook：不被实时编码调用
3. ❌ Mhook直接hook虚函数地址：__thiscall的ecx问题
4. ❌ C++函数指针调用原虚函数：ecx传递错误

### 6.2 成功方案
1. ✅ dumpbin分析导入表，定位LibVOIP10.dll!CreateCBREncoder
2. ✅ Mhook hook CreateCBREncoder（cdecl，无ecx问题）
3. ✅ dumpbin反汇编确认虚函数表[9]=Encode
4. ✅ vtable patch直接修改虚函数表[9]
5. ✅ __fastcall hook + 内联汇编调用原函数（正确传递ecx）
6. ✅ memcpy替换arg1指向的YUV数据
7. ✅ 防重复patch（共享虚函数表）
8. ✅ 动态尺寸适配
9. ✅ U/V平面顺序调整

---

## 7. 后续优化方向

### 7.1 短期
- [x] 定位真实编码入口
- [x] vtable patch稳定hook
- [x] 参数逆向（arg1=YUV数据）
- [x] 静态YUV画面替换测试成功
- [x] 多次切换观看不崩溃
- [x] 动态尺寸适配

### 7.2 中期
- [ ] 图片加载 → 缩放至编码尺寸 → 转YUV420 → 替换
- [ ] 实时捕获屏幕 → 转YUV → 替换（动态假画面）
- [ ] 替换开关控制（主程序UI）
- [ ] 与主程序[截图替换]页面集成

### 7.3 长期
- [ ] 视频文件循环播放作为假画面
- [ ] 64位系统适配
- [ ] 多版本极域适配

### 7.4 风险与限制
1. **32位仅**：LibVOIP10.dll是32位，64位极域需单独分析
2. **版本依赖**：基于V6.0 2016豪华版逆向
3. **性能影响**：memcpy ~1.2MB/帧 × 25fps = 30MB/s，可忽略
4. **U/V顺序**：已通过实测调整，不同版本可能不同

---

## 8. 文件清单

| 文件 | 说明 |
|------|------|
| JiYuTrainerHooks.cpp | DLL源码，含所有hook逻辑 |
| JiYuTrainerHooks.dll | 编译后的DLL（Win32 Release） |
| realtime_analyzer.py | 网络流分析工具 |
| avcodec_probe.log | 探测日志（学生端C:\Users\Public\） |
| 实时监控抓包分析报告.md | 网络层抓包分析 |
| EncodeToJPEGBuffer参数逆向.md | 缩略图编码逆向 |

---

*报告日期：2026-09-14*
*项目：学习不通2.0（JiYuKiller）实时屏幕替换技术预研*
*状态：核心功能验证成功，待集成主程序UI*

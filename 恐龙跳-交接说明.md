# 交接说明：把「恐龙跳（内嵌 dino.html + WebView2）」编译通过并跑起来

> 面向接手者（豆包）。本文自包含，不需要额外上下文。
> 代码由前一位助手编写，**从未编译过**，因此第一步不是写代码而是编译+实机验证。

---

## 一、背景

- 项目：C# WPF，.NET Framework 4.7.2，**老式非 SDK 工程**（csproj 显式列 `<Page>` / `<Compile>`）
- 根目录：`C:\Users\Administrator\Desktop\Jiyuikiller2.0-Comprehensive-Restructuring`
- 解决方案 `JiYuKiller.sln`，项目 `JiYuKiller\JiYuKiller.csproj`
- 主窗口 `MainWindow.xaml`：**450×600 固定 + `ResizeMode="NoResize"`**，且 **`AllowsTransparency="True"` / `WindowStyle="None"` / `Background="Transparent"`**
  （这三点是本次最大的技术风险来源，见第三节第 0 步）
- 小游戏相关文件：
  - `Games\GameMenu.xaml(.cs)` — 游戏菜单（**已完成，别动**）
  - `Games\Minesweeper.xaml(.cs)` — 扫雷（**已完成，别动**）
  - `Games\DinoGame.xaml(.cs)` — 恐龙跳（**本次对象**）
- 导航机制：`MainWindow.xaml` 里 `PageGames` 内是 `ContentControl x:Name="GameContent"`，菜单与游戏都塞进它；**离开页面时 MainWindow 会把 `GameContent.Content` 置空**（用于触发 `Unloaded`，从而释放 WebView2）
- 相关历史入口：`MainWindow.xaml.cs` 的 `ShowPage("games")` / `ResetGameContent()` / `NavGames_Click`

## 二、⚠️ 先读：这件事以前失败过一次

**git 历史显示，这份 `dino.html` 已经被尝试内嵌过一次，并且把程序搞崩了。**

```
7fd8413  feat: 添加小游戏功能-…+恐龙跳WebBrowser加载dino.html+统一UI设计
ee40379  fix: 恐龙跳改用C#纯Canvas实现-移除WebBrowser和dino.html
         (WebBrowser引用外部Google API导致程序崩溃)
b46c510  （reset 回到 WebBrowser 版本）
215859e  feat: 小游戏功能(deepseek生成)-…+扫雷9x9/10雷+恐龙跳纯C# Canvas绘制+集成到主程序   ← 当前 HEAD
```

即：**之前用 IE 内核的 `WebBrowser` 加载这份页面时，页面里的外部 Google API 导致程序崩溃**，于是改成了纯 C# 画形状的版本。

这次改用 **WebView2（Chromium 内核）** 重新内嵌，并且**已经在网络层把所有外部请求全部拦掉**（见第四节第 5 点），就是为了不再重演同样的崩溃。但请注意：**这不代表风险已经排除，必须实机验证。**

## 三、git 状态与「保命」操作

当前仓库：分支 `main`，HEAD = `215859e`。

**关键：前一位助手的所有改动都还没有提交，全在工作区里。**

动手前建议先保住现状（否则改坏了没法回退）：

```bash
git status
git checkout -b feat/dino-webview2
git add -A
git commit -m "wip: 恐龙跳改为内嵌 dino.html + WebView2（尚未编译验证）"
```

之前那版**纯 WPF 形状绘制**的恐龙跳可以从 git 取回：

```bash
# 只看内容，不改工作区
git show HEAD:JiYuKiller/Games/DinoGame.xaml
git show HEAD:JiYuKiller/Games/DinoGame.xaml.cs

# 或者直接恢复这两个文件（会丢掉本次 WebView2 改动）
git checkout HEAD -- JiYuKiller/Games/DinoGame.xaml JiYuKiller/Games/DinoGame.xaml.cs
```

动手前先确认 HEAD 里那版确实是形状绘制版：

```bash
git log --oneline -5 -- JiYuKiller/Games/DinoGame.xaml
git ls-files | grep -i dino      # 看 dino.html 是否已被跟踪
```

> 注：如果决定放弃 WebView2 方案，**退回形状绘制版也是一条正当退路**，而且没有 airspace 风险。

## 四、任务顺序

### 第 0 步（最高优先级）：确认 WebView2 在透明主窗口上到底能不能显示

主窗口是 `AllowsTransparency="True"` 的**分层窗口（`WS_EX_LAYERED`）**，而 WebView2 是**原生 HWND 控件**，子 HWND 不会被合成进分层窗口。可能结果有两种：

- **A. 显示成不透明矩形** —— 圆角/半透明失效，但游戏能玩、可接受
- **B. 完全不显示** —— 只看得到 `#FFF7F7F7` 的空 Border，看不到游戏

**不实机跑一次无法判断。** 编译通过后第一件事就是看恐龙跳页面有没有画面。

如果落到 **B**，补救方向（按推荐顺序）：

1. **把游戏放进独立的普通窗口**：新建一个 `Window`（`AllowsTransparency=False`、背景设不透明 `#F7F7F7`），「恐龙跳」入口改为打开这个窗口，而不是塞进 `PageGames`。这是最干净的解法，彻底绕开 airspace 问题。
2. 关掉主窗口的 `AllowsTransparency`（会牺牲整个液态玻璃观感，**不建议**）。

如果落到 **A**：接受；或把 `PageGames` 区域背景改成不透明以弱化割裂感。

### 第 1 步：补上缺失的 NuGet 包（**当前编译不过的唯一原因**）

#### 1.1 现状：项目里根本没有 NuGet

已核查确认，这个仓库**完全没有 NuGet 基础设施**：

| 检查项 | 结果 |
|---|---|
| `packages.config` | ❌ 不存在 |
| `<PackageReference>` | ❌ csproj 里一条都没有 |
| `packages\` 目录 | ❌ 不存在 |
| 任意 `*.nupkg` | ❌ 全盘没有 |
| `nuget.config` | ❌ 不存在 |
| 全局包缓存 `C:\Users\Administrator\.nuget\packages` | ❌ 目录不存在 |
| `%APPDATA%\NuGet` | ❌ 不存在 |

所以 **`Microsoft.Web.WebView2` 的托管程序集一个都没有**，而恐龙跳现在依赖它：

- `DinoGame.xaml` 第 6 行：`xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"`
- `DinoGame.xaml.cs`：`using Microsoft.Web.WebView2.Core;` 以及 `CoreWebView2Environment`、`CoreWebView2`、`CoreWebView2NavigationCompletedEventArgs`、`WebView2RuntimeNotFoundException` 等类型

**这就是当前编译不过的全部原因。** 装完包就应该只剩 0 错误（其余代码已通过两轮静态审查）。

#### 1.2 这台机器的构建工具情况（会影响你选哪条路）

已核查：

- ✅ `C:\Windows\Microsoft.NET\Framework\v4.0.30319\MSBuild.exe`（.NET Framework 自带的 MSBuild，可以编译本项目）
- ✅ `C:\Program Files\dotnet\dotnet.exe`（但**只有 `shared` 运行时，没有 `sdk` 目录** → 没有 .NET SDK，不能用 `dotnet add package`）
- ❌ **没有 Visual Studio**（`Program Files` 与 `Program Files (x86)` 下都找不到 `devenv.exe`；只剩 `Installer\vswhere.exe`）
- ❌ 没有 `nuget.exe`
- ❌ 没有 NuGet 包缓存

> ⚠️ 注意：以上是**编写本文的沙箱环境**的探测结果。**如果你（豆包）运行在用户真正的开发机上，可能装了 Visual Studio** —— 那就直接走 1.3 的 A 路线，最省事。先自己 `where devenv` / `where nuget` 确认一下。

#### 1.3 A 路线：有 Visual Studio（推荐）

工具 → NuGet 程序包管理器 → 程序包管理器控制台，默认项目选 **JiYuKiller**：

```
Install-Package Microsoft.Web.WebView2
```

装完确认 csproj 里出现三样东西（**缺一不可**）：

1. `Microsoft.Web.WebView2.Core` 与 `Microsoft.Web.WebView2.Wpf` 两个 `<Reference>`
2. 一行 `<Import Project="..\packages\Microsoft.Web.WebView2.<版本>\build\Microsoft.Web.WebView2.targets" ... />`
3. 生成了 `packages.config`

#### 1.4 B 路线：没有 VS / 没有 NuGet 客户端

需要**联网**把包弄下来（本机无缓存，离线装不了）。任选一种：

**B-1 用 nuget.exe（最接近 A 路线）**

```cmd
:: 下载 CLI（需要联网）
curl -L -o nuget.exe https://dist.nuget.org/win-x86-commandline/latest/nuget.exe
:: 装到仓库的 packages 目录（老式工程的标准位置）
nuget.exe install Microsoft.Web.WebView2 -OutputDirectory packages
```

它只会把包解压到 `packages\Microsoft.Web.WebView2.<版本>\`，**不会自动改 csproj**，所以还要手工做 B-3。

**B-2 直接下载 .nupkg 解压**

从 https://www.nuget.org/packages/Microsoft.Web.WebView2 下载 `.nupkg`（本质是 zip），
解开后把内容放到 `packages\Microsoft.Web.WebView2.<版本>\`，保证里面能看到
`lib\net45\Microsoft.Web.WebView2.Core.dll`、`lib\net45\Microsoft.Web.WebView2.Wpf.dll`
和 `build\Microsoft.Web.WebView2.targets`。

**B-3 手工接线（A/B-1/B-2 之后都要做）**

新建 `packages.config`（放在 `JiYuKiller\` 下，版本号按实际目录名替换）：

```xml
<?xml version="1.0" encoding="utf-8"?>
<packages>
  <package id="Microsoft.Web.WebView2" version="<实际版本>" targetFramework="net472" />
</packages>
```

在 csproj 的 `</Project>` 之前、`<Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />` **之后**加：

```xml
  <Import Project="..\packages\Microsoft.Web.WebView2.<实际版本>\build\Microsoft.Web.WebView2.targets"
          Condition="Exists('..\packages\Microsoft.Web.WebView2.<实际版本>\build\Microsoft.Web.WebView2.targets')" />
```

在已有的 `<Reference>` 那个 ItemGroup 里加两条（`HintPath` 用相对 `JiYuKiller\` 的路径）：

```xml
    <Reference Include="Microsoft.Web.WebView2.Core">
      <HintPath>..\packages\Microsoft.Web.WebView2.<实际版本>\lib\net45\Microsoft.Web.WebView2.Core.dll</HintPath>
      <Private>True</Private>
    </Reference>
    <Reference Include="Microsoft.Web.WebView2.Wpf">
      <HintPath>..\packages\Microsoft.Web.WebView2.<实际版本>\lib\net45\Microsoft.Web.WebView2.Wpf.dll</HintPath>
      <Private>True</Private>
    </Reference>
```

> 老式 csproj 里 `<Reference Include>` 只写简单名 + `HintPath` 就够了，**不需要**写 `Version=` / `PublicKeyToken=`。

#### 1.5 两个必须知道的坑

⚠️ **坑 1：不能只手写 `<Reference>`。**
只加 `<Reference>` 能**编译通过**，但**运行时会崩**——因为原生 `WebView2Loader.dll` 不会被拷到输出目录。
必须带上 1.3 第 2 点 / 1.4 B-3 的 `targets` 导入。
万一 targets 用不上，退路是**手工把 `WebView2Loader.dll`（x86 和 x64 各一份，来自包的 `runtimes\win-x86\native\`、`runtimes\win-x64\native\`）复制到 exe 同级目录**。

⚠️ **坑 2：运行时还要有 WebView2 Runtime（和 NuGet 包是两回事）。**
NuGet 包只是编译期的 SDK；**运行期**需要目标机器装 WebView2 Runtime。
本机已装：`C:\Program Files (x86)\Microsoft\EdgeWebView\Application\151.0.4129.101\`。
没装的机器上控件会显示提示文字，**不会崩**（代码里专门 catch 了 `WebView2RuntimeNotFoundException`）。

#### 1.6 如果包实在装不上

那就**放弃 WebView2 方案，从 git 退回纯 C# 形状绘制版**（它没有外部依赖，一定能编译）：

```bash
git checkout HEAD -- JiYuKiller/Games/DinoGame.xaml JiYuKiller/Games/DinoGame.xaml.cs
```

（记得同时把 csproj 里那条 `<EmbeddedResource Include="..\dino.html">` 一起决定去留。）
**这是完全正当的退路**，而且没有第四节第 0 步的 airspace 风险。

### 第 2 步：编译并修错

装包前的预期错误：`DinoGame.xaml` 里 `MC3074: The tag 'WebView2' does not exist in XML namespace ...`，以及 `DinoGame.xaml.cs` 里若干 `CS0246` / `CS0234`。**装完包应该全部消失。**

如果还有别的错误，重点核对第八节列出的 API 清单。

### 第 3 步：内嵌资源名契约（改一边必须同时改另一边）

```xml
<!-- JiYuKiller.csproj -->
<EmbeddedResource Include="..\dino.html">
  <LogicalName>JiYuKiller.Games.dino.html</LogicalName>
</EmbeddedResource>
```
```csharp
// Games\DinoGame.xaml.cs
private const string HtmlResourceName = "JiYuKiller.Games.dino.html";
```

`dino.html` **故意留在仓库根目录**（不在项目目录内），用 `..\dino.html` 引用——已确认为合法 MSBuild，`LogicalName` 原样生效。

想让它出现在 VS 项目树里：把文件移进 `JiYuKiller\Games\`，同步把 `Include` 改成 `Games\dino.html`。

### 第 4 步：运行验收清单

路径：主程序 → 导航栏「小游戏」→「恐龙跳」

- [ ] **游戏区能看到画面**（第四节第 0 步，头条验收项）
- [ ] 显示原版页面：上方 `Press space to start` + 下方恐龙游戏画面
- [ ] 按空格 → 恐龙开始跑、世界滚动
- [ ] **按住**空格 = 高跳；**轻点**空格 = 矮跳
- [ ] ↑ 也能跳；↓ 跳跃中加速下落；回车重开
- [ ] 撞仙人掌 → `GAME OVER` + 重开图标
- [ ] 顶部「分数」徽标随游戏变化
- [ ] 「重新开始」→ 整页重载、游戏重置
- [ ] 「← 返回」→ 回到小游戏菜单
- [ ] 返回后任务管理器里 **`msedgewebview2.exe` 已退出**（验证 `Dispose()` 生效）
- [ ] 返回后切到别的页面，**空格键恢复正常**（不再被吞）
- [ ] 再进一次「恐龙跳」，能正常再次加载
- [ ] 扫雷仍然正常（回归测试）
- [ ] **全程无外部网络请求**（可用 Fiddler/防火墙观察，见第四节第 5 点）

### 第 5 步：可能需要收尾的点

1. **分数徽标读不到**：徽标每 700ms 执行 `Runner.instance_.distanceRan` 取值。若一直显示 `分数: 0`，最省事的做法是**直接删掉顶部那个分数徽标**（`DinoGame.xaml` 里 `ScoreText` 所在的 `Border`）——游戏画面右上角本来就有原版自己的分数。
2. **键盘没反应**：说明焦点没进网页。当前只在加载完成后调 `Browser.Focus()`。可补 `Browser.CoreWebView2Controller.MoveFocus(...)` 加强（审查标记此点"需实机确认"）。
3. **布局细微差异（已知）**：`dino.html` 的 `<body id="t">` 没有 `offline` 类，所以 `.offline .runner-container` 那组绝对定位规则（第 2476–2503 行）不生效，画布按普通流排在提示文字下方，而不是像真 Chrome 那样绝对定位到 `top:10px`。**能玩。** 想改就给 body 加 `offline` 类，但注意那组规则里 `.interstitial-wrapper` 的 `margin-top` 是 100px，会把游戏往下推，小面板里未必更好看。
4. **文案是英文原版**（`Press space to start` / `GAME OVER`）。要中文化就改 `dino.html` 对应 DOM 文本，或在注入脚本里替换。

## 五、已经做完的事（不要重复做）

### 1. 已修掉 3 个真实 bug

| bug | 说明 | 现状 |
|---|---|---|
| `ShowFallback` 单向门 | 折叠 `Browser` 后没有任何地方恢复，触发过一次 fallback 就永久空白，连之后成功加载也渲染进不可见控件 | 加载成功和「重新开始」时都会恢复 `Visible` |
| 把被自己拦掉的跳转当成加载失败 | `NavigationStarting` 拦 http/https 后，`NavigationCompleted` 会以 `IsSuccess=false` / `WebErrorStatus=OperationCanceled` 触发，被误判为页面加载失败并走 fatal 分支 | 已特判 `OperationCanceled` 并忽略 |
| 初始化竞态 | 用户在 `EnsureCoreWebView2Async` 等待期间点「返回」，`Cleanup` 释放控件后 catch 分支里的 `ShowFallback` 会碰已释放控件 | `ShowFallback` 与两个 catch 都加了 `_disposed` 保护；分数定时器 catch 改为完全静默 |

### 2. 已加「彻底断网」（针对历史上的崩溃原因）

`InitializeBrowserAsync` 里已经注册：

```csharp
core.AddWebResourceRequestedFilter("*", CoreWebView2WebResourceContext.All);
core.WebResourceRequested += CoreWebView2_WebResourceRequested;   // 403 拦掉所有 http/https
```

配合原有的 `NavigationStarting`（拦 frame 跳转）与 `NewWindowRequested`（拦弹窗），运行期**一个外部请求都不会发出去**。

> ⚠️ 这段 API 用法**未经编译验证**。如果编译报错，见第八节。

### 3. 已确认所有 WebView2 API 名称/签名正确

见第八节。**不用再逐个怀疑。**

### 4. 已确认其它模块不受影响

`GameMenu.xaml.cs` 只调 `new DinoGame()`（无参构造，已保留）；没有任何地方引用已删除的旧 DinoGame 成员；`GameContent` 是普通 `ContentControl`；扫雷用的是同样的返回方式；csproj 其它 Page/Compile 项未动。

## 六、绝对不要做的事

- ❌ **不要用「读取 → 重写」的方式拷贝或格式化 `dino.html`。**
  文件里有几行**超长 base64**：第 2059–2102 行的 CSS 图标、第 2579–2581 行的三段 MP3 音效，**单行都超过 2000 字符**。很多工具/编辑器会截断这些行，**会损坏音效数据导致游戏报错**。要动就用能保留原字节的方式（或根本不改文件，用运行时注入补丁）。
- ❌ **不要退回用 WPF 的 `WebBrowser`（IE11）。** 两个独立原因：
  1. 它 UA 不含 `chrome`，而页面末尾的引导是 `if (navigator.userAgent.indexOf('chrome') > -1) new Runner(...)`，所以只会显示 "Sorry, this game only runs on Google Chrome!"，游戏根本不启动；
  2. 它没有 `AudioContext` 和 `<template>.content`，第一次跳跃就抛异常；
  3. **而且历史上就是它把程序搞崩过**（见第二节）。
- ❌ **别动 `Games\Minesweeper.*`、`Games\GameMenu.*`、`MainWindow.*`**
  （除非第四节第 0 步落到 B 方案、需要新增一个独立窗口）。`GameMenu.xaml.cs` 里是 `new DinoGame()`，保持这个无参构造签名。

## 七、页面本身的关键事实（已核实）

- 引导脚本在 `dino.html` 第 2586 行：`if (navigator.userAgent.toLowerCase().indexOf('chrome') > -1) { new Runner('.interstitial-wrapper'); }`
- **WebView2 的 UA 含 `Chrome/…`**（Edge UA），所以判断成立、游戏会启动；
  `IS_MOBILE` 为 false，键盘/鼠标监听会正常挂上；精灵图与音效全是内联 `data:` URI，**完全离线可跑**。
- 注入脚本的时机正确：`AddScriptToExecuteOnDocumentCreatedAsync` 在文档创建时执行（此时 `readyState === 'loading'`），所以 `DOMContentLoaded` 的判断是必要的、写法正确。
- 注入脚本只隐藏两类东西，**不会影响游戏**：
  - `#socialbutts`（Facebook / Twitter / Google+ 外链组件盒）
  - 那个空的 `thecodepost` 链接块（第 2557 行，占着 80px 高度；其 `<a>` 未闭合、由 `</div>` 隐式闭合，选择器能匹配到）
  可见的 `Press space to start`（第 2556 行）**被正确保留**。
- `NavigateToString` 有约 2MB 上限；本页面按可见 base64 估算在 100–250KB 量级，**远低于上限**。

## 八、已确认无误的 WebView2 API 清单

以下全部已逐一核对（名称、签名、所属程序集），**不用再怀疑**：

| 用法 | 位置 |
|---|---|
| `xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"` | `DinoGame.xaml`（控件类型 `Microsoft.Web.WebView2.Wpf.WebView2` 确实在该程序集） |
| `CoreWebView2Environment.CreateAsync(browserExecutableFolder, userDataFolder, options)` → `(null, userDataFolder, null)` | 参数顺序正确 |
| `Browser.EnsureCoreWebView2Async(env)` | 存在 |
| `CoreWebView2.Settings.AreDefaultContextMenusEnabled / AreDevToolsEnabled / IsStatusBarEnabled / IsZoomControlEnabled` | 四个都存在（`[Core]`） |
| `CoreWebView2.AddScriptToExecuteOnDocumentCreatedAsync(string)` | 存在 |
| `WebView2.NavigateToString(string)` | 存在 |
| `Browser.NavigationCompleted` + `CoreWebView2NavigationCompletedEventArgs.IsSuccess / .WebErrorStatus` | 存在，处理器签名正确 |
| `CoreWebView2.NewWindowRequested` + `CoreWebView2NewWindowRequestedEventArgs.Handled` | 存在 |
| `CoreWebView2.NavigationStarting` + `CoreWebView2NavigationStartingEventArgs.Uri / .Cancel` | 存在 |
| `CoreWebView2.WebResourceRequested` + `CoreWebView2WebResourceRequestedEventArgs.Request.Uri / .Response` | 存在 |
| `CoreWebView2.AddWebResourceRequestedFilter(string, CoreWebView2WebResourceContext)` | 存在 |
| `Browser.CoreWebView2.Environment.CreateWebResourceResponse(Stream, int, string, string)` | 存在（拦截请求的标准写法，第一个参数传 `null` 表示空 body） |
| `CoreWebView2.ExecuteScriptAsync(string)` | 存在 |
| `WebView2RuntimeNotFoundException`（在 `Microsoft.Web.WebView2.Core`） | 存在；catch 顺序（具体→`Exception`）正确 |
| `WebView2.Dispose()` | 存在（释放底层 COM 资源，包括 `CoreWebView2`） |

**唯一两个"不确定、需实机确认"的点（审查明确标注为 unsure，不是猜测）：**
1. `Dispose()` 之后再访问 `CoreWebView2` 是抛 `ObjectDisposedException` 还是返回 `null`——不影响正确性，因为所有访问都在 `try/catch` 里且加了 `_disposed` 保护；
2. `Browser.Focus()` 是否足以把键盘输入送进 WebView2 的 HWND（很多时候需要先点一下）——需实机验证。

## 九、验收标准

1. 编译 0 错误；
2. 第四节第 0 步有明确结论（能显示 / 不能显示），若不能显示则按 B 方案改造；
3. 第四节第 4 步清单逐项通过；
4. 离开页面后 `msedgewebview2.exe` 退出；
5. 扫雷等其它页面不受影响；
6. 运行期无外部网络请求。

# JiYuKiller 2.0 全面重构 - 更新日志

## QD_V2.0_JiYuRebuild-TEST (2026-09-08)

### 真正毛玻璃效果（移植自 WPF-Liquid-Glass-Effect-main）
- **桌面截图背景**：GDI 全屏截图，截取窗口后方桌面作为背景
- **像素着色器折射**：HLSL 编译着色器（GlassyEffect.ps），实时折射/模糊/边缘光晕
- **自动刷新背景**：窗口移动、大小变化、激活/失活时自动重新截图
- **三层结构**：截图背景层 + 玻璃着色器层 + 半透明内容层
- **光感效果**：渐变边框光效（白→蓝→粉→白）+ 顶部高光
- **外层光晕虚化**：双层光晕（淡蓝+白色），模拟玻璃边缘光线散射
- **圆角窗口**：ContentLayer 圆角裁剪，消除直角

### 自定义设置页面（底栏新增入口）
- 模糊强度调节（0-100%）
- 内容层透明度调节（5%-100%）
- 外层光晕透明度调节（0-100%）
- 中层光晕透明度调节（0-100%）
- 刷新玻璃背景按钮

### 新增功能
- **快捷栏页面**：底栏新增快捷栏入口，整合当前状态、极域控制、窗口控制、电源控制
- **本窗口置顶**：SetWindowPos P/Invoke 切换窗口置顶
- **杀死极域**：Process.Kill 结束 StudentMain 进程
- **重启极域**：杀死后重新启动，支持自定义路径和常见默认路径
- **极域状态实时显示**：运行中(PID) / 未运行，颜色区分
- **电源控制**：关闭计算机 / 重新启动 / 退出软件
- **帮助文档页面**：移植原项目 HelpWindow.html，包含介绍/快捷键/其他/免责声明
- **软件高级参数配置**：移植原项目 ConfigWindow 全部17项设置
- **调试命令执行**：支持 help/killst/rerunst/status/whereisi/shutdown/reboot/exit/clear/refresh

### UI 优化
- 底栏改为6按钮布局：快捷栏 / 控制台和设置 / 自定义设置 / 帮助文档 / 调试 / 关于
- 设置页面分组卡片式布局：进程控制 / 窗口控制 / 教师权限
- 当前状态和电源控制从设置页面移至快捷栏
- 调试页面三行布局：工具栏 / 调试命令 / 日志显示
- 全局字体加大
- 软件高级设置按钮颜色：紫色→红色
- 取消退出确认弹窗
- 窗口宽度 800→450
- 默认参数：模糊强度100%、内容透明度50%、外层光晕70%、中层光晕90%

### 修复
- 毛玻璃效果不显示：GlassyLayer 背景绑定截图层、pack URI 程序集名修正、管理器初始化时机
- 窗口四角黑色空隙：移除 AllowsTransparency 窗口上的 DropShadowEffect（Windows 10 已知问题）
- RadioButton 误用 CheckBox 样式导致 XAML 解析崩溃
- XAML 中文引号导致解析错误
- TextBox 不支持 CornerRadius 属性
- 底栏按钮文字间距不够被遮挡

### 程序信息
- 程序显示名称：学习不通
- 进程名：i.chaoxing
- 程序图标：JiYuTrainerLogo.ico
- 版本号：QD_V2.0_JiYuRebuild-TEST
- 目标框架：.NET Framework 4.7.2

## QD_V1.1_TrayIcon (2026-09-07)

### 新增功能
- **系统托盘支持**：关闭/最小化按钮改为隐藏到托盘
- **托盘双击**：显示/隐藏主窗口
- **托盘右键菜单**：显示主界面 / 退出软件
- **窗口不可最大化**：ResizeMode=NoResize

### 修复
- 命名空间冲突：System.Drawing.Color vs System.Windows.Media.Color
- 图标文件复制：Resource 改为 Content

## QD_V1.0_WPF-Rebuild (2026-09-07)

### 项目重构
- 从 C++/Sciter 技术栈迁移至 **C# .NET Framework 4.7.2 + WPF**

### 已实现功能
- **统一日志系统**：`.log` 后缀，exe 同目录，详细记录
- **设置持久化**：XML 格式
- **极域进程监控**：后台线程自动扫描 StudentMain
- **禁止极域运行**：检测到后自动结束
- **屏幕广播置顶控制**：移除 WS_EX_TOPMOST
- **极域路径定位**：手动指定 + 常见路径自动检测
- **调试模式**：内置日志查看器

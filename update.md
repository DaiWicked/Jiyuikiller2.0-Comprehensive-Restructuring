# JiYuKiller 2.0 全面重构 - 更新日志

## QD_V1.0_WPF-Rebuild (2026-09-07)

### 项目重构
- 从 C++/Sciter 技术栈迁移至 **C# .NET Framework 4.7.2 + WPF**
- 全新项目结构，模块化设计，便于后续功能扩展

### 已实现功能
- **全局 Liquid Glass 毛玻璃 UI**：半透明玻璃背景，可配置透明度和背景色
- **统一日志系统**：`.log` 后缀，exe 同目录，详细记录按钮点击/复选框变化/窗口事件/函数调用
- **设置持久化**：XML 格式保存到 exe 目录，支持恢复默认
- **极域进程监控**：后台线程自动扫描 StudentMain 进程
- **禁止极域运行**：检测到极域进程后自动结束
- **屏幕广播置顶控制**：自动移除极域窗口的 WS_EX_TOPMOST 属性
- **极域路径定位**：文件选择对话框手动指定，支持常见路径自动检测
- **调试模式**：内置日志查看器，实时显示最后500行日志

### UI 页面
- **控制台和设置**：7项极域控制开关 + 操作按钮
- **调试**：日志实时查看 + 调试模式开关
- **关于**：版本信息 + 作者信息

### 复用资源
- 主程序图标：JiYuTrainerLogo.ico（原项目）

### 已知限制（后续版本实现）
- 禁止极域结束进程/关闭窗口（需 Hook DLL）
- 允许教师监视/控制（需 Hook DLL）
- 解除网络控制（需内核驱动）
- 软件高级配置页面

### 技术细节
- 目标框架：.NET Framework 4.7.2
- UI 框架：WPF (XAML)
- 日志编码：UTF-8 (无 BOM)
- 编译工具：MSBuild v145

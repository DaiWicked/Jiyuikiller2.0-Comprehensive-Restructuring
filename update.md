# JiYuKiller 2.0 全面重构 - 更新日志

## QD_V2.0_JiYuRebuild-TEST (2026-09-08)

### 新增功能
- **快捷栏页面**：底栏新增快捷栏入口，整合当前状态、极域控制、窗口控制、电源控制
- **本窗口置顶**：SetWindowPos P/Invoke 切换窗口置顶，按钮文字和颜色同步变化
- **杀死极域**：Process.Kill 结束 StudentMain 进程
- **重启极域**：杀死后重新启动，支持自定义路径和常见默认路径
- **极域状态实时显示**：运行中(PID) / 未运行，颜色区分
- **电源控制**：关闭计算机 / 重新启动 / 退出软件，关机重启前自动停止控制器
- **帮助文档页面**：移植原项目 HelpWindow.html，包含介绍/快捷键/其他/免责声明4个子页面
- **软件高级参数配置**：移植原项目 ConfigWindow 全部17项设置
  - 驱动设置、极域附加控制、程序隐藏、快捷键、结束进程模式、注入模式、高级程序配置、控制器启停
  - 可直接使用：隐藏托盘图标、控制器启停、CKInterval检查间隔
- **调试命令执行**：支持 help/killst/rerunst/status/whereisi/shutdown/reboot/exit/clear/refresh 命令，回车快速执行

### UI 优化
- 底栏改为5按钮布局：快捷栏 / 控制台和设置 / 帮助文档 / 调试 / 关于
- 设置页面重新排版为分组卡片式布局：进程控制 / 窗口控制 / 教师权限
- 当前状态和电源控制从设置页面移至快捷栏
- 调试页面三行布局：工具栏 / 调试命令 / 日志显示
- 全局字体加大：按钮/复选框/输入框 13→14，所有文字统一+1~2号
- 软件高级设置按钮颜色：紫色→红色(#DC3545)
- 取消退出确认弹窗，点击退出直接关闭

### 修复
- RadioButton 误用 CheckBox 样式导致 XAML 解析崩溃
- XAML 中文引号导致解析错误，改用「」
- TextBox 不支持 CornerRadius 属性
- 底栏按钮文字间距不够被遮挡，字体和内边距调整

### 名称统一
- 程序显示名称统一为"学习不通"
- 进程名：i.chaoxing
- 版本号：QD_V2.0_JiYuRebuild-TEST

## QD_V1.1_TrayIcon (2026-09-07)

### 新增功能
- **系统托盘支持**：关闭/最小化按钮改为隐藏到托盘，不退出程序
- **托盘双击**：显示/隐藏主窗口
- **托盘右键菜单**：显示主界面 / 退出软件
- **首次隐藏提示**：气泡提示
- **窗口不可最大化**：ResizeMode=NoResize

### 修复
- 命名空间冲突：System.Drawing.Color vs System.Windows.Media.Color
- 图标文件复制：Resource 改为 Content

## QD_V1.0_WPF-Rebuild (2026-09-07)

### 项目重构
- 从 C++/Sciter 技术栈迁移至 **C# .NET Framework 4.7.2 + WPF**

### 已实现功能
- **全局 Liquid Glass 毛玻璃 UI**
- **统一日志系统**：`.log` 后缀，exe 同目录，详细记录
- **设置持久化**：XML 格式
- **极域进程监控**：后台线程自动扫描 StudentMain
- **禁止极域运行**：检测到后自动结束
- **屏幕广播置顶控制**：移除 WS_EX_TOPMOST
- **极域路径定位**：手动指定 + 常见路径自动检测
- **调试模式**：内置日志查看器

### UI 页面
- 控制台和设置：7项极域控制开关
- 调试：日志查看 + 调试模式
- 关于：版本信息 + 作者信息

### 复用资源
- 主程序图标：JiYuTrainerLogo.ico

### 技术细节
- 目标框架：.NET Framework 4.7.2
- UI 框架：WPF (XAML)
- 日志编码：UTF-8

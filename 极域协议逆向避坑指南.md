# 极域电子教室协议逆向与工具开发避坑指南

> 本文档记录了在极域电子教室（V4.0 2016 / V6.0 2016）协议逆向与工具开发过程中，前作者与现团队反复踩过的坑。希望后续开发者能避免重复这些错误。

---

## 一、极域学生端进程架构

| 进程 | 权限 | 作用 |
|------|------|------|
| **GATESRV.exe** | SYSTEM（Windows服务，会话0） | 网络通信、接收教师端命令、执行远程命令 |
| **StudentMain.exe** | 当前登录用户（会话1） | UI显示、黑屏、广播界面 |
| **MasterHelper.exe** | 当前用户 | 辅助进程 |

### 关键陷阱：会话0隔离

GATESRV.exe作为服务运行在**会话0**，它用`CreateProcess`启动的程序也在**会话0**中运行。用户登录在**会话1**，看不到会话0的窗口，也无法交互。

- `notepad.exe`：进程在会话0启动了，但用户看不到窗口（容易误以为"没执行"）
- 抓屏程序在会话0启动：**无法抓取用户桌面**（会话0没有桌面）
- 关机/重启：不需要桌面交互，所以能成功

---

## 二、协议包类型与常见误用

### 2.1 MESS包（Magic=0x5353454D）

- **用途**：黑屏/解锁、消息等
- **特点**：**不需要教师端登录握手**，伪造即可生效
- **已知限制**：黑屏约5秒后学生端自动解除（检测到发包者不是当前会话教师端）
- **正确用法**：发送后约5秒有效，适合"看个样子"的场景

### 2.2 MSG包

- **用途**：向学生端发送消息
- **特点**：不需要登录握手，伪造即可生效

### 2.3 REBOOT/SHUTDOWN包

- **用途**：关机/重启
- **特点**：不需要登录握手，伪造即可生效
- **注意**：这是专门的控制包，不是CMD包

### 2.4 DMOC包（Magic=0x444D4F43）—— 最常见的错误

- **前作者的错误认知**：认为DMOC包offset 100写入命令字符串就能执行任意命令
- **实际情况**：学生端收到DMOC包后，将offset 100的字符串**自动拼接成 `C:\Windows\system32\<命令>`**，然后用CreateProcess执行
- **后果**：
  - `notepad.exe` → `C:\Windows\system32\notepad.exe` → 能执行（但在会话0）
  - `cmd /c echo test > file.txt` → 路径错误，执行失败
  - `certutil -urlcache ...` → 参数被忽略或解析失败
  - 任何带参数、带`cmd /c`、带`&&`的命令都无法正确执行
- **结论**：DMOC包**只能执行system32目录下的纯可执行文件名**，不支持参数和复合命令

### 2.5 COMD包（Magic=0x434F4D44）—— 正确的远程命令执行协议

- **用途**：远程运行程序（cmdId=0x0F）、打开网页/文件（cmdId=0x18）
- **特点**：
  - 路径和参数是**UTF-16LE定长字段**（路径256 wchar，参数160 wchar）
  - 支持任意绝对路径
  - 服务态用`CreateProcessAsUser`在用户会话中启动
- **关键限制**：**需要教师端完成登录握手后学生端才会执行**
  - teacher_sim的run命令中有检查：`if sip not in students: 学生未登录`
- **结论**：纯UDP伪造工具（如UdpGhost）无法使用COMD命令，因为没有完成登录握手

---

## 三、已验证做不通的方向

### 3.1 黑屏保持

- **现象**：发送MESS黑屏包后，学生端约5秒自动解除
- **原因**：学生端检测到发包者不是当前会话的教师端，自动解除黑屏
- **尝试过**：每3秒重发黑屏包 → 无效
- **结论**：无法保持黑屏，只能"闪一下"

### 3.2 UdpGhost远程部署程序

- **尝试过的方案**：
  1. `cmd /c certutil下载 + start运行` → DMOC包不支持cmd /c
  2. `certutil下载 + schtasks /it` → DMOC包不支持参数
  3. 下载到`C:\Users\Public\` → 下载命令本身不执行
- **根本原因**：DMOC包只能执行system32下的纯文件名，COMD包需要登录握手
- **结论**：纯UDP伪造工具无法远程部署任意程序，需手动部署或用teacher_sim

### 3.3 UdpGhost实时监控学生屏幕

- **原因**：学生端不主动给非教师端发送视频流
- **替代方案**：DolbyVision独立监控系统（在学生机运行发送端，UdpGhost作为接收端）

### 3.4 关闭allowMonitor后截图替换

- **现象**：关闭"允许教师监视电脑"后，教师端查看屏幕时极域学生端崩溃
- **原因**：原作者已知问题，32位Win10驱动兼容性
- **结论**：不建议在此方向继续投入

---

## 四、可行的方向

### 4.1 teacher_sim（完整教师端模拟）

- 完成登录握手后可使用全部教师端功能
- 支持COMD远程命令执行（任意路径、带参数）
- 支持实时监控、黑屏、关机、消息等
- 限制：需要抢占教师端身份，可能与真实教师端冲突

### 4.2 UdpGhost（纯UDP伪造）

- 不需要登录握手，不抢占教师端身份
- 可执行：黑屏（约5秒）、发消息、关机、重启
- 不可执行：远程命令、远程部署、实时监控
- 适合：快速恶作剧、不影响真实教师端的场景

### 4.3 DolbyVision（独立屏幕监控）

- 完全绕过极域协议
- 在被观看电脑运行发送端（手动部署），接收端通过TCP观看
- 支持多客户端、MJPEG流
- 适合：需要实时监控但不想触碰极域协议的场景

### 4.4 截图替换（虚假反监视）

- Hook `EncodeToJPEGBuffer`，在JPEG编码完成后替换输出数据
- 不依赖关闭allowMonitor，不触碰GDI/驱动
- 支持缩略图替换（80x60）和实时视频流替换
- 实时替换支持图片和视频
- 注意：应用缩略图替换后需重启极域学生端生效

---

## 五、开发建议

1. **先抓包验证，再写代码**：不要凭猜测构造协议包，用Wireshark抓真实教师端的包对比
2. **区分协议包类型**：DMOC ≠ COMD，不要混用
3. **注意会话0隔离**：服务启动的程序用户看不到，需要用`CreateProcessAsUser`或schtasks /it
4. **测试简单命令先行**：先测试`notepad.exe`能否执行，再测试复杂命令
5. **手动部署优先**：远程部署受协议限制，手动部署最可靠
6. **保留日志**：avcodec_probe.log和dll_load.log对调试截图替换很有帮助

---

## 六、版本历史中的关键错误

| 版本 | 错误 | 后果 | 修复 |
|------|------|------|------|
| 早期 | 用DMOC包offset 100写任意命令 | 复杂命令不执行 | 认清DMOC包限制 |
| 中期 | 尝试certutil+schtasks远程部署 | 命令不执行 | 删除远程部署，改用手动 |
| 中期 | 黑屏保持（每3秒重发） | 学生端5秒自动解除 | 接受限制，默认5秒 |
| 后期 | 关闭allowMonitor做截图替换 | 学生端崩溃 | 改用Hook EncodeToJPEGBuffer |

---

*本文档基于V4.0 2016和V6.0 2016版本的逆向分析，其他版本可能有差异。*

<Window x:Class="GameRoom.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GameRoom" Height="520" Width="760"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="CanResize"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">

    <Window.Resources>
        <!-- 面板样式 -->
        <Style x:Key="GlassPanel" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource PanelBg}" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="BorderBrush" Value="{DynamicResource GlassBorder}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <!-- 主色按钮 -->
        <Style x:Key="AccentBtn" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource AccentFg}" />
            <Setter Property="Background" Value="{DynamicResource Accent}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="bd" CornerRadius="6" Background="{TemplateBinding Background}" Padding="12,6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <Border x:Name="hover" CornerRadius="6" Background="#26FFFFFF" Opacity="0" IsHitTestVisible="False" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard><Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hover" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15" />
                                    </Storyboard></BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard><Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="hover" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15" />
                                    </Storyboard></BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 幽灵按钮 -->
        <Style x:Key="GhostBtn" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="bd" CornerRadius="6" Background="{TemplateBinding Background}" Padding="10,5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <Border x:Name="hover" CornerRadius="6" Background="#14FFFFFF" Opacity="0" IsHitTestVisible="False" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="hover" Property="Opacity" Value="1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- 外层圆角窗口 -->
    <Border Background="{DynamicResource WindowBg}" CornerRadius="12" BorderBrush="{DynamicResource WindowBorder}" BorderThickness="1">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Grid Grid.Row="0" Background="{DynamicResource HeaderBg}" MouseLeftButtonDown="TitleBar_MouseDown">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0">
                    <Ellipse Width="10" Height="10" Fill="{DynamicResource Accent}" Margin="0,0,8,0"/>
                    <TextBlock Text="GameRoom" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TitleNick" Text="" FontSize="11" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" Margin="8,2,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,8,0">
                    <Button Content="—" Style="{StaticResource GhostBtn}" Click="MinBtn_Click" Width="32" Height="28" FontSize="14"/>
                    <Button Content="✕" Style="{StaticResource GhostBtn}" Click="CloseBtn_Click" Width="32" Height="28" FontSize="12"/>
                </StackPanel>
            </Grid>

            <!-- 主区域 -->
            <Grid Grid.Row="1" Margin="12,8,12,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 在线玩家 -->
                <Border Grid.Column="0" Style="{StaticResource GlassPanel}" Margin="0,0,8,0">
                    <DockPanel Margin="10">
                        <TextBlock DockPanel.Dock="Top" Text="在线玩家" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>
                        <TextBlock DockPanel.Dock="Top" x:Name="UserCount" Text="0人" FontSize="11" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,8"/>
                        <ListBox x:Name="UserList" Background="Transparent" Foreground="{DynamicResource TextPrimary}" BorderThickness="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                    </DockPanel>
                </Border>

                <!-- 游戏大厅 -->
                <Border Grid.Column="1" Style="{StaticResource GlassPanel}">
                    <DockPanel Margin="14">
                        <TextBlock DockPanel.Dock="Top" Text="游戏大厅" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10"/>
                        
                        <!-- 游戏选择区（占位，后续加游戏卡片） -->
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="🎮" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            <TextBlock Text="游戏开发中" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Center"/>
                            <TextBlock Text="已连接的玩家可以在此处开始游戏" FontSize="12" Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                        </StackPanel>
                    </DockPanel>
                </Border>
            </Grid>

            <!-- 底部状态栏 -->
            <Border Grid.Row="2" Background="{DynamicResource HeaderBg}" CornerRadius="0,0,12,12" Padding="14,8">
                <DockPanel>
                    <TextBlock x:Name="LogText" Text="就绪" FontSize="11" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center"/>
                    <Button Content="刷新列表" Style="{StaticResource GhostBtn}" Click="Refresh_Click" DockPanel.Dock="Right" Padding="8,3"/>
                </DockPanel>
            </Border>
        </Grid>
    </Border>
</Window>
